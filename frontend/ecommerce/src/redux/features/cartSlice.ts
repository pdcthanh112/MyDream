// import { createAsyncThunk, createSlice } from "@reduxjs/toolkit";
// import * as cartApi from '@apis/cartItemApi'

// // export const addToCart = createAsyncThunk('cart/addToCart', async (requestData, thunkAPI) => {
// //     const {productId:string, quantity:number, cartId:string} = requestData
// //     try {
// //       const response = await cartApi.addToCart(productId, quantity, cartId);
// //       return response.data;
// //     } catch (error) {
// //       return thunkAPI.rejectWithValue(error);
// //     }
// //   });


// const cartSlice = createSlice({
//     name: 'cart',
//     initialState: {},
//     reducers: {},
//     // extraReducers: (builder) {

//     // }
// })

// export default cartSlice.reducer;